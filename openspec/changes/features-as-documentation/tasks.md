## 1. Census and sort (foundation — do first)

- [x] 1.1 Load the groovy + spock convention skills before writing any Groovy/Spock code, and re-read `design.md`.
- [x] 1.2 Build the feature→{chapter, example, e2e} census matrix (design D7): enumerate every user-facing mechanism, map it to the current chapter/example/e2e, and mark each cell present / gap / unsupported.
- [x] 1.3 Sort the 16 `strategies-builtin/.../e2e/` specs into **doc-example** (keep/rework), **repatriate-to-unit** (engine correctness), or **drop** (already unit-covered), recording the destination per spec.
- [x] 1.4 Empirically resolve the uncertain capability cells by writing the smallest example and compiling it (e.g. `Optional<Set<X>>→Set<X>`, nested `List<Set<X>>→Set<List<X>>`); record each as *document-as-supported* or *document-the-limitation*.

## 2. Mechanism spike (behavioural doc-e2e + page co-location)

- [x] 2.1 Spike one behavioural doc example end-to-end (design D2): real example source in a module → real processor with `-Apercolate.docTags` → instantiate + run the generated mapper and assert the result → materialise real generated output → collect → `include::` by tag on a page.
- [x] 2.2 Resolve the open question (design D3): confirm the antora-collector can import module-owned `.adoc` **pages** into the single `percolate` component, or add the needed content source; wire a `scan` entry for `<module>/src/docs`.
- [x] 2.3 Lock the doc-e2e pattern (source-set/tagging/harness) as the template every later chapter reuses; confirm it is `@Tag('integration')` and outside the unit/pitest/coverage gate.

## 3. Repatriate engine correctness to processor unit tests (before any deletion)

- [x] 3.1 For each repatriate-marked spec (`TwoSameTypedSources`, `SourcePathAccessorAmbiguity`, `ContainerReturn`, `OverloadedConstructorAssembly`, `HoistAssembly`, `ContainerStream`, `EndToEndCodegen`): write a `processor` unit test asserting the invariant at the mock seam. (6 of 7 already covered by existing unit specs, verified and deleted; `ContainerStream` reclassified to a doc-example — see census.md §3.1 note — deferred to task 5.1.)
- [x] 3.2 For each diagnostics spec (`ConstantDefaultDiagnostics`, `NoSilentSourcing`, `NullnessCrossing`): assert the diagnostic as a `processor`/`spi` unit test at the validation seam. (`ConstantDefaultDiagnostics`/`NoSilentSourcing` already covered, deleted; `NullnessCrossing`'s codegen-rendering gap closed by extending `NullnessCrossingSpec`, then deleted.)
- [x] 3.3 Confirm each new unit test kills the mutation its e2e implied (mutate the fix, watch the unit test fail), then delete the corresponding e2e — never before. (Live mutation-kill verified for `NullnessCrossingSpec` and `ValidateConstantDefaultLegalityStageSpec`; the rest confirmed by reading exact matching assertions against the e2e's literal invariant.)
- [x] 3.4 Verify no surviving e2e exists solely to assert an engine invariant or a compile diagnostic. (All 7 remaining specs — `ConstantsAndDefaults`, `ContainerStream` (transitional), `ConversionMethod`, `DefaultMethodConversion`, `DocExamples`, `SourcePathChain`, `TypeConversion` — are doc-example track, none is correctness-only.)

## 4. Rework the genuine doc examples (behavioural + single-sourced)

- [x] 4.1 Convert `ConversionMethod` and `DefaultMethodConversion` e2e to behavioural assertions; materialise their real generated output and make the pages `include::` it (replacing the hand-typed blocks).
- [x] 4.2 Split the `DocExamplesEndToEndSpec` grab-bag into per-chapter behavioural specs, each backing exactly one section. (`nested-paths`/`map-annotation`/`collections` each converted to real-source doc examples; grab-bag file deleted.)
- [x] 4.3 Promote the latent doc specs (`ConstantsAndDefaults`, `TypeConversion`, `SourcePathChain`) to curated fixtures + behavioural assertions + single-sourced output. (`ConstantsAndDefaults`' new mechanism — Optional-source `.orElse` fallback — folded into `AccountMapper`; `SourcePathChain` dropped as redundant; `TypeConversion` dropped as already unit-covered, numeric-widening representative recorded as an open doc gap for 7.4.)

## 5. Write the expanded and missing chapters (co-located, each with single-sourced output)

- [x] 5.1 Rewrite Collections as a progression with a worked, single-sourced example per mechanism: `List<X>→List<Y>`, `Set→List`, `Stream→Set`, and presence composed in/out of a container; cross-reference the element conversion. (`TeamMapper`'s `toRoster` adds the composed/nested-container mechanism — `List<Optional<X>>→Optional<Set<Y>>` — replacing `ContainerStreamEndToEndSpec`, now deleted.)
- [x] 5.2 Add the Optionals chapter (wrap, unwrap, composed with containers) with worked examples. (`PreferencesMapper`/`OptionalsDocExampleSpec` witness wrap/unwrap; composed-with-container cross-references `collections.adoc`'s `toRoster`, per D1 — no duplicate witness. Also found and fixed a dead Antora scan entry — `strategies-builtin/build/generated-doc-examples`, from the fully-retired legacy e2e pattern — whose stale leftover output was colliding with and silently blanking the real `collections.adoc` code block; removed the entry and the stale directory.)
- [x] 5.3 Add the Defaults & nullness chapter (JSpecify crossings) with worked examples. (`OrderMapper`/`NullnessDocExampleSpec` witness the `requireNonNull` guard when a `@Nullable` source has no `defaultValue`; cross-references `map-annotation.adoc`'s `requireNonNullElse`/`orElse` fallback forms rather than duplicating them, per D1.)
- [x] 5.4 Add the Path-access section covering getter, record accessor, and public field in one user-facing section. (New `== Path access forms` section in `nested-paths.adoc`; `ContactMapper`/`PathAccessDocExampleSpec` witness all three forms — `getEmail()`, a real Java `record` accessor `areaCode()`, and a public field `zip` — in one path chain.)
- [x] 5.5 Add the Compile-time-switches reference co-located in `processor`, each option shown with its example and materialised generated effect (an `@Tag('integration')` processor doc-e2e per design D4). (New `processor/src/docs/compile-time-switches.adoc`; `CompileTimeSwitchesDocExampleSpec` covers all 5 options — `docTags`, `locals.final`, `locals.var`, `nullable.annotations`, `debug.graphs` — via real `PercolateProcessor` compile-testing runs, materialising each generated variant to `processor/build/generated-doc-examples/switches/`, since comparing an option on vs off needs multiple compilations an ordinary `compileTestJava` run can't express; `processor` gained a scoped test-only dependency on `strategies-builtin`/`test-foundation`/`compile-testing`, per D4 — unit suite stays strategy-free, ArchUnit verified green.)
- [x] 5.6 Rewrite the Extending page co-located in `spi`, using the real `reactor` strategy as the worked example, backed by a behavioural e2e; add the `reactor-blocking` blocking-bridge page. (`extending.adoc` now `include::`s reactor's real `JustOrEmpty.java` (replacing the fictional `MyStrategy` skeleton) plus a real `PreferenceMapper`/`ExtendingDocExampleSpec` in `spi` proving a third-party strategy needs no special engine wiring — `spi` gained a scoped test-only dependency on `percolate`/`reactor`, per D4. New `reactor-blocking/src/docs/reactor-blocking.adoc` + `ReactorBlockingDocExampleSpec` — one representative block-then-map case, compile-testing-based like the switches reference since the module has no source of its own to hang a real fixture on; the existing exhaustive `ReactorBlockingEndToEndSpec` engine-correctness matrix is untouched, cross-referenced not duplicated. **Correction (`/opsx:verify` catch):** `spi`'s doc-e2e used the real-source pattern, not compile-testing, so it needs no `test-foundation` edge — design.md's D4 diagram named one anyway; updated the diagram to match the actual, simpler dependency.)

## 6. Co-locate and thin the spine

- [x] 6.1 Move each feature page from `docs/` into its owning module's `src/docs`, collected via the `scan` entries; verify the rendered site is unchanged. (7 pages moved to `strategies-builtin/src/docs`, 1 to `reactor/src/docs`, 1 to `spi/src/docs`; new `antora.yml` scan entries added per module; `./gradlew antora` green, zero warnings, all moved pages spot-checked rendering their real content.)
- [x] 6.2 Reduce `docs/modules/ROOT/pages/` to the spine (index, getting-started, mapper-structure) and update `nav.adoc` with xrefs into the module pages. (Satisfied as a byproduct of 6.1 — the directory now holds exactly the 3 spine pages; `nav.adoc`'s xrefs were already unaffected by the physical move, since Antora aggregates one flat component regardless of page source location.)
- [x] 6.3 Sweep every remaining page for hand-typed generated-output blocks and replace them with materialised `include::`s (or remove the shown output). (Swept all 14 pages — the 3 spine pages plus every module-owned page; every code block claiming to be generated output is a real `include::`. `index.adoc`'s "A taste" snippet is the hand-typed *source* declaration, never claimed as generated output, so it is exempt by design. **Correction (`/opsx:verify` catch):** the initial pass over-claimed here — `reactive.adoc` showed *zero* generated-output blocks (not a hand-typed one, but not a materialised one either), missed because "no block" reads as trivially compliant. Fixed: `ReactiveDocExampleSpec` now materialises with `docTags=true` and the page `include::`s it, matching every sibling page.)

## 7. Verify and gate

- [x] 7.1 `./gradlew antora` builds green; a broken `include::`/`xref:` fails the build. (Green, zero warnings, after the co-location move in 6.1. Two real silent-breakage cases surfaced and fixed this pass: the stale `generated-doc-examples` collision under 5.2, and (found only by `/opsx:verify`, since it produced no warning) `reactive.adoc` missing its generated-output `include::` entirely — see 6.3.)
- [x] 7.2 `./gradlew check` is green; confirm pitest/coverage still run on the unit suite only and the repatriated unit tests are counted, while every doc-e2e is `@Tag('integration')` and excluded. (Full `./gradlew check` green — fixed 2 real violations surfaced along the way: a spotless formatting drift in `SelfCallGuard.java`/`PreferencesMapper.java`, and 2 codenarc violations — a stray `wrapNullness()` placement in `ContainerSpec.groovy` and an unused import + `UnnecessaryGetter` in `CompileTimeSwitchesDocExampleSpec.groovy`. `pitest.includedGroups = ['unit']` confirmed unchanged; every new doc-e2e this session is `@Tag('integration')`.)
- [x] 7.3 Confirm the `module-boundaries` ArchUnit guards stay green (main-scope, unchanged) and no processor/spi doc-e2e references a `processor.internal..` type. (`architecture-tests` green after every new test-scope edge added this pass — processor→strategies-builtin/test-foundation, spi→percolate/reactor; grep confirms zero `processor.internal` references from any `docs` doc-e2e package.)
- [x] 7.4 Close the census matrix: every documented feature has a behavioural example; every supported mechanism without a section is recorded as a gap or removal candidate. (All 26 mechanism rows now `present`; zero removal candidates. Closed the one carried-over open item — numeric widening — by adding `AccountMapper.balanceCents` (`int→long`) to `map-annotation.adoc`. Recorded the two engine bugs found and fixed while building the Optionals chapter as a new census row, distinct from a documentation gap.)
