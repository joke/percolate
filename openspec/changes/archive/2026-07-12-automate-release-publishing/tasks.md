## 1. Version derivation

- [x] 1.1 Add `org.shipkit.shipkit-auto-version` to root `build.gradle`'s `pluginManagement`/`plugins` block
- [x] 1.2 Replace the hardcoded `version = '0.1.0-SNAPSHOT'` in the `subprojects { }` block with `version rootProject.version` (or remove entirely if the plugin sets `rootProject.version` directly, matching `spock-deepmock`'s `allprojects` pattern)
- [x] 1.3 Verify `./gradlew properties -q | grep version` (or equivalent) resolves a sensible pre-release version with no tags present

## 2. Artifact naming and versionMapping cleanup

- [x] 2.1 Delete the `selfNamedArtifacts` list and `publishedArtifactId` derivation from root `build.gradle`'s `pluginManager.withPlugin('maven-publish')` block
- [x] 2.2 Remove the `artifactId = publishedArtifactId` assignments (both the `components.java` and `components.javaPlatform` publication blocks) so `artifactId` defaults to `project.name`
- [x] 2.3 Remove the `versionMapping { usage('java-api') { fromResolutionResult() }; usage('java-runtime') { fromResolutionResult() } }` block
- [x] 2.4 Run `./gradlew publishToMavenLocal` and confirm coordinates in `~/.m2/repository/io/github/joke/percolate/` match the unprefixed names (`bom`, `spi`, `processor`, `reactor`, `reactor-blocking`, `strategies-builtin`, `annotations`, `percolate`, `percolate-javapoet`)

## 3. Fix the `:dependencies` POM leak at its source

- [x] 3.1 In `spi/build.gradle`, remove the platform edges that leak into the published POM: deleted the (now-redundant, nothing else used `implementation`) `implementation platform(project(':dependencies'))` and the `api platform(project(':dependencies'))` on the main sourceSet, and — discovered mid-task, since `java-test-fixtures` variants merge into the same flat Maven POM as the main component — also deleted `testFixturesImplementation platform(project(':dependencies'))` (compile-time version resolution for its `spock-core`/`groovy` deps still comes from the retained `testFixturesCompileOnly platform(...)`, which participates in `compileClasspath` resolution)
- [x] 3.2 Run `./gradlew :spi:compileJava :spi:check` and confirm `spi` still compiles and its own unit/integration suites pass — green, plus spot-checked `:processor:compileTestJava` and `:strategies-builtin:compileTestJava` (both consume `testFixtures(project(':spi'))`)
- [x] 3.3 Delete the `pom.withXml { asNode().dependencyManagement.each { it.parent().remove(it) } }` block from root `build.gradle`
- [x] 3.4 Run `./gradlew publishToMavenLocal` and inspect `spi`'s generated POM to confirm no `<dependencyManagement>` import of the internal `:dependencies` platform remains — confirmed, the block is gone entirely

## 4. POM metadata via the metadata plugin

- [x] 4.1 Add `io.github.sgtsilvio.gradle.metadata` (and `io.github.sgtsilvio.gradle.maven-central-publishing`) to root `build.gradle`'s `pluginManagement`/`plugins` block
- [x] 4.2 Declare a single `metadata { }` block (readable name, description, `license { apache2() }`, `developers { register("joke") { fullName = "Joke de Buhr"; email = "joke@xckk.de" } }`, `github { org = "joke"; }`) once at the root, scoped to apply across every module that applies `maven-publish` — `github.repo` explicitly pinned to `rootProject.name` ("percolate"), since the plugin's own default convention derives repo from each module's own `project.name` (would have produced wrong per-module URLs like `github.com/joke/spi`)
- [x] 4.3 Remove any now-redundant hand-written POM population left over from the existing `publishing { publications { maven(MavenPublication) { ... } } }` block once the metadata plugin supplies it — nothing left; groups 2/3 already stripped it down to bare `from components.java`/`from components.javaPlatform`
- [x] 4.4 Run `./gradlew publishToMavenLocal` and inspect a generated POM (e.g. `spi`) to confirm name/description/license/developer/SCM fields are present — verified on both `spi` (java) and `bom` (java-platform)

## 5. Central Portal publishing wiring

- [x] 5.1 Apply `io.github.sgtsilvio.gradle.maven-central-publishing` across every module that applies `maven-publish`, confirming it targets the Central Portal endpoint (no OSSRH staging URL, no GitHub Packages repository) — confirmed via `./gradlew tasks --all`: every publishable module gets its own `publishToMavenCentral`/`mavenCentralBundle`/`cleanMavenCentralStagingRepository` tasks, no `publishing.repositories` entry added
- [x] 5.2 Apply core `signing`, configured with `useInMemoryPgpKeys(findProperty('signingKey'), findProperty('signingPassword'))`, required whenever the task graph includes `publish` — gate written as `required { gradle.taskGraph.allTasks.any { it.name.contains('MavenCentral') } }` since this plugin's real publish task is `publishToMavenCentral`, not `publish` (corrected in proposal/design/specs too)
- [x] 5.3 Enable `withSourcesJar()`/`withJavadocJar()` on every publishable Java module (mirroring `spock-deepmock`'s `java { withJavadocJar(); withSourcesJar() }`) — discovered mid-task that `javadoc` had never actually run on this codebase: Lombok's `onConstructor_ = @Inject` trick is unresolvable under javadoc's own parser (23 sites in `processor`, hard compile-like errors, not just doclint warnings), which produced empty javadoc jars even with doclint suppressed. Asked the user how to handle it; they chose the proper fix over suppression: added `io.freefair.gradle:lombok-plugin` (`io.freefair.lombok`, pinned to the existing `1.18.46`), which wires `javadoc` to compile against **delomboked** (expanded, Lombok-free) source instead of raw source. This also replaces every module's manual `annotationProcessor`/`compileOnly 'org.projectlombok:lombok'` version-less declaration with the plugin's own centrally-versioned `lombok` configuration — except the explicit declaration had to stay (re-added) in every module: the plugin's `extendsFrom`-based wiring alone doesn't guarantee Lombok resolves first on the annotationProcessor path, and `processor` (which also runs `dagger-compiler`) broke with `Dagger/MissingBinding` until Lombok was declared first again. Javadoc jars are now fully populated (159 files/~5.9MB for `processor`, 108/~5.3MB for `spi`), no doclint suppression needed.
- [x] 5.4 Confirm (by inspection, since real credentials aren't available yet) that `mavenCentralUsername`/`mavenCentralPassword` and `signingKey`/`signingPassword` are the only credential properties the build expects, matching what the user will add as GitHub secrets later — confirmed via the plugin's own README/source (`MetadataExtensionImpl`/`maven-central-publishing` README); no other credential property is referenced anywhere in the build

## 6. Release workflow

- [x] 6.1 Add `.github/workflows/release.yml` with a `release-please` job using `GoogleCloudPlatform/release-please-action`, `release-type: simple`, authenticated with a `RELEASE_PLEASE` secret token, exposing `release_created`/`tag_name` outputs
- [x] 6.2 Add a `publish` job, `needs: release-please`, gated on `if: ${{ needs.release-please.outputs.release_created }}`, checking out the release tag, setting up Java, and running `./gradlew check` then `./gradlew publishToMavenCentral` — used `jdx/mise-action@v4` + `gradle/actions/setup-gradle@v5` to match the existing `build.yml`'s toolchain-provisioning pattern (`.mise.toml` pins the JDK), not `actions/setup-java`
- [x] 6.3 Wire `ORG_GRADLE_PROJECT_mavenCentralUsername`/`Password` and `ORG_GRADLE_PROJECT_signingKey`/`signingPassword` env vars on the `publish` step from the corresponding (not-yet-created) GitHub secrets
- [x] 6.4 Confirm no step in the workflow can run `publish` on a non-release trigger (review the job's `if:` condition and its dependency on `release-please`'s output) — verified: `publish` has `needs: release-please` + `if: ${{ needs.release-please.outputs.release_created }}`, so it's skipped entirely unless release-please actually cut a release; YAML validated (python yaml.safe_load + pre-commit's check-yaml/pretty-format-yaml hooks, both passed)

## 7. Documentation and verification

- [x] 7.1 Update README.md's consumer usage snippet from `io.github.joke.percolate:percolate-bom` / `percolate-annotations` to the unprefixed coordinates (`io.github.joke.percolate:bom`, `io.github.joke.percolate:annotations`) — also swept the Antora manual (`getting-started.adoc`, `extending.adoc`, `collections.adoc`, `reactive.adoc`, `reactor-blocking.adoc`), `spi`/`strategies-builtin` module READMEs, and doc-example source comments referencing the old prefixed names; `percolate-javapoet` (exempt, its own directory name) and `percolate-smoke`/internal `dependencies` (unpublished) left untouched
- [x] 7.2 Run `./gradlew check` and confirm it is green, including the `consumer-packaging` module-boundary and smoke checks — full `./gradlew check publishToMavenLocal` green after two unrelated hiccups fixed along the way: a stale Gradle daemon threw a transient `NoClassDefFoundError` in spotless (resolved by `./gradlew --stop`, not a real regression), and one genuine `palantir-java-format` violation from a doc-string edit (fixed via `spotlessApply`). Final coordinate check: `~/.m2/repository/io/github/joke/percolate/` contains exactly `annotations, bom, percolate, percolate-javapoet, processor, reactor, reactor-blocking, spi, strategies-builtin` (no `-dependencies` platform published), and `spi`'s POM has zero `dependencyManagement` occurrences
