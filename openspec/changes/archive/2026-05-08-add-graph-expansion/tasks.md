## 1. Dependency and module setup

- [x] 1.1 Add `com.google.auto.service:auto-service-annotations` (compile) and `com.google.auto.service:auto-service` (annotationProcessor) to `processor/build.gradle`
- [x] 1.2 Confirm `processor` module compiles after dependency addition (no usage yet)
- [x] 1.3 Verify the alignment change's `Weights`, `EdgeCodegen`, `IncomingValues`, `VarNames`, `GroupCodegen`, `MapperGraph.addGroupCodegen` / `groupCodegen` / `realisedSubgraph()` are all present and used internally; no schema drift

## 2. SPI package surface

- [x] 2.1 Create `io.github.joke.percolate.processor.spi` package with `package-info.java` carrying `@org.jspecify.annotations.NullMarked`
- [x] 2.2 Re-export the alignment change's codegen interfaces from `processor.spi`: `EdgeCodegen`, `GroupCodegen`, `IncomingValues`, `VarNames` (either by `package-private` re-publishing types in `processor.spi`, or by relocating the originals into `processor.spi` if no consumer outside the spi/graph packages uses them)
- [x] 2.3 Create `Step` Lombok `@Value` record with fields `TypeMirror produces`, `int weight`, `EdgeCodegen codegen`
- [x] 2.4 Create `BridgeStep` Lombok `@Value` record with fields `int weight`, `EdgeCodegen codegen`
- [x] 2.5 Create `Slot` Lombok `@Value` record with fields `String name`, `TypeMirror type`, `int weight`
- [x] 2.6 Create `GroupBuild` Lombok `@Value` record with fields `List<Slot> slots`, `GroupCodegen codegen`
- [x] 2.7 Create `ResolveCtx` interface with exactly two methods: `Types types()`, `Elements elements()`
- [x] 2.8 Create `SourceStep` interface with `Stream<Step> stepsFrom(TypeMirror, String, ResolveCtx)` and `default int priority() { return 0; }`
- [x] 2.9 Create `GroupTarget` interface with `Optional<GroupBuild> buildFor(TypeMirror, List<String>, ResolveCtx)` and `default int priority() { return 0; }`
- [x] 2.10 Create `Bridge` interface with `Optional<BridgeStep> bridge(TypeMirror, TypeMirror, ResolveCtx)` and `default int priority() { return 0; }`
- [x] 2.11 Spock unit specs: each `@Value` record's getter returns its constructor argument; each interface's `priority()` default returns `0`

## 3. Built-in strategies

- [x] 3.1 Create package `io.github.joke.percolate.processor.spi.builtins` with `package-info.java` carrying `@NullMarked`
- [x] 3.2 Implement `GetterRead` (`@AutoService(SourceStep.class)`) — finds `getX()` / `isX()` accessors matching `pathTail`, excludes Object members, emits `Step(returnType, Weights.STEP, codegen)` with codegen `<input>.<methodName>()`
- [x] 3.3 Implement `ConstructorCall` (`@AutoService(GroupTarget.class)`) — exact match constructor whose parameter-name set equals `Set.copyOf(targetTails)`; emits `GroupBuild` whose slots are in constructor-declaration order with `Weights.STEP` weight; codegen renders `new T(<positional inputs>)`
- [x] 3.4 Implement `DirectAssign` (`@AutoService(Bridge.class)`) — `Types.isSameType(s, t)` ⇒ emit `BridgeStep(Weights.NOOP, identityCodegen)`; otherwise empty
- [x] 3.5 Verify `META-INF/services/io.github.joke.percolate.processor.spi.SourceStep` (etc.) are generated by `auto-service` after a `gradle :processor:compileJava`
- [x] 3.6 Architectural Spock spec: scan compiled bytecode of `GetterRead`, `ConstructorCall`, `DirectAssign` and assert no class reference to `processor.graph.*` or `processor.expand.*`

## 4. Stage interface and Pipeline refactor

- [x] 4.1 Create `Stage` interface (or abstract base) in `io.github.joke.percolate.processor` with a single method that takes a per-mapper context carrier
- [x] 4.2 Create `MapperContext` carrier (Lombok `@Data` or `@Value` with setters where needed) holding the `TypeElement`, the discovered abstract methods list, the `MapperMappings`, the `MapperGraph`, and the `Diagnostics` scar state
- [x] 4.3 Update existing stage classes (`DiscoverAbstractMethods`, `DiscoverMappings`, `ValidateNoDuplicateTargets`, `ValidateSourceParameters`, `SeedGraph`, `DumpGraph`) to implement `Stage` (read inputs from context, write outputs to context); preserve current per-class behaviour
- [x] 4.4 Refactor `Pipeline` to take a single `List<Stage> stages` injection and iterate `stages.forEach(s -> s.run(ctx))` inside `process(TypeElement)`; construct a fresh `MapperContext` per call
- [x] 4.5 Add `ProcessorModule` `@Provides List<Stage>` method returning the nine stages in declared order: `[DiscoverAbstractMethods, DiscoverMappings, ValidateNoDuplicateTargets, ValidateSourceParameters, SeedGraph, DumpGraph, ExpandStage, ValidateRealisationStage, DumpExpandedGraph]`
- [x] 4.6 Update `Pipeline` Spock specs to reflect the new constructor shape and the nine-stage ordering scenario
- [x] 4.7 Confirm all existing pipeline integration tests still pass (the new ExpandStage / ValidateRealisationStage / DumpExpandedGraph implementations are no-op shells at this point — tasks below replace the no-ops)

## 5. Expansion phases

- [x] 5.1 Create package `io.github.joke.percolate.processor.expand` with `@NullMarked` `package-info.java`
- [x] 5.2 Create `ExpansionPhase` interface with `MapperGraph apply(MapperGraph graph)` (returning the same instance after mutation)
- [x] 5.3 Implement `ResolveSourceChainsPhase`: iterate SEED edges with source-side `to` nodes; for each, pull typed FROM (direct or via MARKER); call every `SourceStep` with `(typedSourceType, pathTail, ctx)`; for each `Step`, allocate a typed `Node`, emit `Edge.realised(...)`, emit `Edge.marker(...)`; iterate to internal fixed point
- [x] 5.4 Implement `ResolveTargetChainsPhase`: iterate typed return-type root nodes; aggregate `targetTails` in declaration-source-position order; call every `GroupTarget` with `(returnType, targetTails, ctx)`; for each `GroupBuild`, allocate a fresh `groupId`, allocate typed slot nodes, emit `Edge.realised(...)` for each slot sharing `groupId`, register the `GroupCodegen` via `MapperGraph.addGroupCodegen`, emit MARKER edges for each slot
- [x] 5.5 Implement `BridgeSourceToTargetPhase`: iterate flavor-② SEED edges; resolve realised counterparts on each side (typed-at-seed or via MARKER); skip when either side has zero realisations (readiness predicate); for each `(typedFrom, typedTo)` pair, call every `Bridge` with `(fromType, toType, ctx)`; for each match, emit `Edge.realised(...)`. No new nodes, no MARKER edges in this phase
- [x] 5.6 Implement `ExpandStage` (`Stage`) — invoke the three phases in order; after each phase run JGraphT `CycleDetector` over the SEED+SUB_SEED subgraph, scar mapper on cycle; track per-seed sub-directive lineage count; abort+scar lineage exceeding budget=100
- [x] 5.7 Implement driver-side SUB_SEED forward-compat path (skeleton only — no v1 strategy emits sub-directives, but the queue-and-budget machinery is in place)
- [x] 5.8 Add `ProcessorModule` `@Provides` methods for `List<SourceStep>`, `List<GroupTarget>`, `List<Bridge>` — each calls `ServiceLoader.load(<Iface>.class, <classloader>)` once, sorts by FQN, wraps in `Collections.unmodifiableList`, returns `@Singleton`

## 6. Realisation validation

- [x] 6.1 Create package `io.github.joke.percolate.processor.validate` with `@NullMarked` `package-info.java` (or extend an existing validation package)
- [x] 6.2 Create `ValidationPhase` interface with `void apply(MapperGraph graph, TypeElement typeElement)` (no graph mutation)
- [x] 6.3 Implement `ValidateMarkersPhase` (Tier-2) — iterate every `?`-typed seed node; if outgoing-MARKER count is zero, emit `Diagnostics.error` with the originating SEED's `directive` `AnnotationMirror`, scar the mapper
- [x] 6.4 Implement `ValidatePathsPhase` (Tier-3) — skip scarred mappers; for each flavor-② SEED, walk REALISED forward from source-side counterpart and backward from target-side counterpart; if shoulders not connected, emit one error per SEED with the bidirectional gap diagnostic (both shoulders + missing type pair) keyed to the SEED's `directive`
- [x] 6.5 Implement `ValidateRealisationStage` (`Stage`) — invoke Tier-2 then Tier-3 in order
- [x] 6.6 Verify the existing `Diagnostics` API supports scarring at the `TypeElement` level; if not present, add a `Diagnostics.warning(...)` channel and the scar APIs (the alignment change's design.md flagged this as an open question)

## 7. DumpExpandedGraph stage

- [x] 7.1 Implement `DumpExpandedGraph` (`Stage`) mirroring the existing `DumpGraph` shape — depend on `Filer`, `Diagnostics`, `ProcessorOptions`, `DotRenderer`
- [x] 7.2 Apply the same off-when-option-off, no-file-on-empty-graph, IOException-as-warning behaviours documented for `DumpGraph`
- [x] 7.3 Always write the file when `debugGraphs == true` and the graph is non-empty — including when the mapper was scarred by validation
- [x] 7.4 Use file naming `<MapperFQN>.expanded.dot` via `Filer.createResource(SOURCE_OUTPUT, "", "<FQN>.expanded.dot", originatingElement)`

## 8. DotRenderer extensions

- [x] 8.1 Verify the kind-keyed colour / line-style table from the alignment change actually exercises REALISED, MARKER, and SUB_SEED edges (it was shipped without running edges of those kinds)
- [x] 8.2 Render `groupId` (when present) as an edge attribute or inside the label so grouped edges are visually distinguishable
- [x] 8.3 Confirm `strategyClassFqn` rendering (shipped in alignment) actually surfaces on REALISED, MARKER, and SUB_SEED edges for diagnostic value
- [x] 8.4 Update existing seed-only DOT goldens — none should change (seed-only behaviour is unchanged)
- [x] 8.5 Verify edge ordering remains ascending natural `Edge` order across mixed-kind graphs (no kind-based grouping in the rendered file)

## 9. Test infrastructure

- [x] 9.1 Create `FakeResolveCtx` helper in `processor/src/test/groovy/io/github/joke/percolate/processor/spi/` that wraps Compile Testing's `Types` / `Elements` and implements `ResolveCtx`
- [x] 9.2 Extend the test-only Groovy `MapperGraphExtensions` module to expose: `realisedEdgesFrom(Node)`, `markersOn(Node)`, `groupCodegenOf(String)`, `subGraphView(EdgeKind...)`, `cycleDetector()`-passthrough — used only from spec code
- [x] 9.3 Update `MapperGraphSpec` and any other existing graph-extension consumer to confirm the additions compile cleanly with the existing surface

## 10. Strategy unit specs (consolidated in SpiTypesSpec)

- [x] 10.1 `SpiTypesSpec` — GetterRead: finds public getter, finds boolean is-getter, returns empty for missing accessor, excludes `getClass()`, codegen renders `var.method()` correctly
- [x] 10.2 `SpiTypesSpec` — ConstructorCall: exact match, no subset, no superset, ignores `targetTails` order, slots in constructor-declaration order, codegen renders `new T(...)`
- [x] 10.3 `SpiTypesSpec` — DirectAssign: same-type matches with `NOOP` weight + identity codegen, different types return empty, `List<String>` vs `Collection<String>` returns empty (uses `isSameType`, not `isAssignable`)
- [x] 10.4 `StrategyRegistrationSpec` — `ServiceLoader.load(SourceStep.class)` includes `GetterRead`; provided `List<SourceStep>` is sorted by FQN; loaded once per round
- [x] 10.5 Architectural import spec — `processor.spi.builtins.*` classes import nothing from `processor.graph.*` or `processor.expand.*`

## 11. Phase unit specs

- [x] 11.1 `ResolveSourceChainsPhaseSpec` — single-segment realisation, multi-segment (dotted) realisation iterates correctly, same-side `?→?` skipped until FROM realises, no-match strategy emits zero edges, MARKER weight is `NOOP`
- [x] 11.2 `ResolveTargetChainsPhaseSpec` — exact-match emits group with shared `groupId`, multiple parallel groups when multiple `GroupTarget`s match the same root, no-match emits zero edges, `addGroupCodegen` registered on graph, MARKER edges from seed slots to typed slots
- [x] 11.3 `BridgeSourceToTargetPhaseSpec` — DirectAssign realises identity bridge, skipped when either side has no realisation (readiness predicate), multiple Bridge matches emit parallel REALISED edges, bare-parameter source bridge fires correctly
- [x] 11.4 `ExpandStageSpec` — runs three phases in order, each phase exactly once, `MapperGraph.realisedSubgraph()` excludes SEED+MARKER, no SUB_SEED in v1 demo, no cycles in v1 demo, no budget exhaustion in v1 demo
- [x] 11.5 `ExpandStageCycleSpec` — synthetic graph with constructed SEED+SUB_SEED cycle triggers cycle-detected error and mapper scarring
- [x] 11.6 `ExpandStageBudgetSpec` — synthetic test stub strategy that loops emits the budget-exhausted error after 100 expansions
- [x] 11.7 `ValidateMarkersPhaseSpec` — unrealised `?`-typed seed emits one error per node, error is keyed to the directive's `AnnotationMirror`, mapper is scarred, multiple unrealised seeds emit multiple errors
- [x] 11.8 `ValidatePathsPhaseSpec` — gap diagnostic includes both shoulders and missing type pair, passes when realised path exists, skips scarred mappers, emits at most one error per SEED bridge
- [x] 11.9 `ValidateRealisationStageSpec` — Tier-2 runs first, Tier-3 runs after, scarring from Tier-2 suppresses Tier-3 for that mapper

## 12. End-to-end integration

- [x] 12.1 Add a Compile Testing fixture mapper for the v1 demo (`PersonMapper.map(Person): Human` with two `@Map` directives, `Human` declaring `Human(String firstName, String lastName)` via Lombok or hand-written ctor)
- [x] 12.2 Integration spec: compile the fixture; assert no errors emitted; assert the post-expansion `MapperGraph` contains the expected REALISED + MARKER edges (six REALISED edges across phases, four MARKER edges)
- [x] 12.3 Integration spec: compile a fixture using a dotted target path (`@Map(target = "address.street", ...)`) and assert exactly one Tier-2 error keyed to that directive's `@Map` annotation
- [x] 12.4 Integration spec: compile a fixture with `Person.Address` → `Human.Address` (different concrete types, no Bridge available) and assert exactly one Tier-3 bidirectional gap error referencing both shoulders and the missing type pair `(Person.Address, Human.Address)`
- [x] 12.5 Integration spec: confirm `Pipeline.process` returns `null` (no codegen yet) for all three fixtures

## 13. DOT golden specs

- [x] 13.1 Add a `processor/src/test/resources/golden-graphs/expanded-trivial.dot` golden for the v1 demo mapper's full post-expansion graph (six REALISED edges, four MARKER edges, no SUB_SEED, six SEED edges)
- [x] 13.2 Add a golden for a Tier-2 failing mapper (dotted target) showing seed-with-no-marker rendered alongside the partial source-side realisations
- [x] 13.3 `DumpExpandedGraphSpec` — option off ⇒ no file; option on + non-empty ⇒ `.expanded.dot` written; option on + empty graph ⇒ no file; `Filer.IOException` ⇒ warning; option on + scarred mapper ⇒ file is still written; file name is exactly `<FQN>.expanded.dot`
- [x] 13.4 `DotRendererExpandedSpec` — REALISED edges render with documented styling, MARKER edges render with documented styling, SUB_SEED edges render with documented styling (constructed in test), `groupId` appears in edge attributes when present

## 14. Architectural and regression checks

- [x] 14.1 Architectural test: scan classes in `processor.spi.*` and assert none reference `processor.graph.*` or `processor.expand.*`
- [x] 14.2 Regression: every pre-existing seed-graph spec passes unchanged (no behavioural change for the seed stage)
- [x] 14.3 Regression: every pre-existing DOT golden for seed-only graphs passes unchanged
- [x] 14.4 Run `gradle :processor:test --tests "io.github.joke.percolate.processor.**"` — green
- [x] 14.5 Run `gradle :processor:integrationTest` — green
- [x] 14.6 Run `gradle :processor:check` — green (errorprone, NullAway, palantir-format)

## 15. Verification and archive readiness

- [x] 15.1 Run `openspec validate add-graph-expansion --strict` — clean
- [x] 15.2 Manually inspect a generated `<FQN>.seed.dot` — unchanged from the alignment change baseline
- [x] 15.3 Manually inspect a generated `<FQN>.expanded.dot` for the v1 demo — confirm REALISED edges show GetterRead / ConstructorCall / DirectAssign provenance, MARKER edges link seeds to typed counterparts, group ids appear on the constructor edges
- [x] 15.4 Manually inspect a generated `<FQN>.expanded.dot` for a Tier-3 failing mapper — confirm both shoulders are visually identifiable
- [x] 15.5 Confirm `processor` module bytecode does not contain any reference from `processor.spi.builtins.*` to `processor.graph.*` or `processor.expand.*` (`javap`-based check or bytecode-scanning Spock spec)
- [x] 15.6 Update `openspec/changes/explore-expansion-model/notes.md` with a pointer to this change ("superseded by add-graph-expansion") and consider archiving it
