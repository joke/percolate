plugins {
    id 'java-library'
    id 'maven-publish'
    id 'percolate.conventions'
}

dependencies {
    compileOnly platform(project(':dependencies'))
    compileOnly 'org.jspecify:jspecify'
}

tasks.named('pitest') {
    enabled = false
}
