plugins {
    id 'com.diffplug.gradle.spotless' apply false
    id 'com.palantir.baseline-config' apply false
    id 'net.ltgt.errorprone' apply false
    id 'net.ltgt.nullaway' apply false
    id 'org.danilopianini.cpd' apply false
    id 'info.solidsoft.pitest' apply false
    id 'org.antora'
}

// The Antora user manual is provisioned and run by the org.antora plugin on a managed Node runtime,
// so the site build is a `./gradlew antora` task that participates in the Gradle graph (replacing the
// mise npm:antora pin). The collector extension is installed in Antora's own Node context via packages.
antora {
    version = '3.1.15'
    playbook = 'antora-playbook.yml'
    options = [clean: true, fetch: true]
    packages = [
            '@antora/collector-extension': '1.0.3'
    ]
}

// The doc-example e2e specs materialise the real doc-tagged generated source the manual include::s,
// so the site build must run them first; the collector then scans their output into the catalog.
tasks.named('antora') {
    dependsOn ':strategies-builtin:integrationTest'
}

subprojects { project ->
    group = 'io.github.joke.percolate'
    version = '0.1.0-SNAPSHOT'

    repositories {
        mavenCentral()
    }

    pluginManager.withPlugin('java-base') {
        pluginManager.apply 'com.diffplug.spotless'
        pluginManager.apply 'com.palantir.baseline-encoding'
        pluginManager.apply 'com.palantir.baseline-java-compiler-diagnostics'
        pluginManager.apply 'com.palantir.baseline-java-properties'
        pluginManager.apply 'com.palantir.baseline-module-jvm-args'
        pluginManager.apply 'jacoco'
        pluginManager.apply 'net.ltgt.errorprone'
        pluginManager.apply 'pmd'
//        pluginManager.apply 'org.danilopianini.cpd'
        // pluginManager.apply 'com.palantir.baseline-format'
        tasks.withType(JavaCompile).configureEach {
            options.release = 11
            options.compilerArgs << '-Werror'
            options.fork = true
            options.forkOptions.jvmArgs += [
                    '--sun-misc-unsafe-memory-access=allow'
            ]
        }
    }

    pluginManager.withPlugin('com.diffplug.spotless') {
        spotless {
            java {
                forbidWildcardImports()
                removeUnusedImports()
                palantirJavaFormat('2.87.0')
            }
        }
    }

    pluginManager.withPlugin('java') {
        pluginManager.apply 'com.palantir.baseline-test-heap'
        pluginManager.apply 'com.palantir.baseline-testing'

        tasks.named('test') {
            useJUnitPlatform {
                includeTags 'unit'
            }
        }

        tasks.register('integrationTest', Test) {
            useJUnitPlatform {
                includeTags 'integration'
            }
            testClassesDirs = sourceSets.test.output.classesDirs
            classpath = sourceSets.test.runtimeClasspath
        }

        tasks.named('check') {
            dependsOn 'integrationTest'
        }
        tasks.withType(Test).configureEach {
            maxParallelForks = Runtime.runtime.availableProcessors().intdiv(2) ?: 1
        }
    }

    pluginManager.withPlugin('groovy') {
        pluginManager.apply 'codenarc'
        codenarc {
            configFile = file("$rootDir/.codenarc.groovy")
            maxPriority1Violations = 0
            maxPriority2Violations = 0
            maxPriority3Violations = 0
        }
        dependencies {
            codenarc platform(project.project(':dependencies'))
            codenarc 'org.codenarc:CodeNarc'
        }
        tasks.withType(CodeNarc).configureEach {
            reports {
                xml.required = false
                html.required = true
                text.required = true
            }
        }
    }

    pluginManager.withPlugin('info.solidsoft.pitest') {
        pitest {
            outputFormats = ['XML', 'HTML']
            pitestVersion = '1.25.1'
            junit5PluginVersion = '1.2.3'
            mutators = ['ALL']
            enableDefaultIncrementalAnalysis = true
            includedGroups = ['unit']
            timestampedReports = false
            threads = (project.findProperty('pitestThreads') ?: 1) as int
            if (project.findProperty('pitestTargetClasses')) {
                targetClasses = project.findProperty('pitestTargetClasses').split(',') as List
            }
            if (project.findProperty('pitestTargetTests')) {
                targetTests = project.findProperty('pitestTargetTests').split(',') as List
            }
            // pitest's minion JVMs do not inherit the Test task's parallel-disabling system properties and would
            // otherwise read the developer's global ~/.spock config (parallel + optimizeRunOrder on). The engine
            // seam specs share a static, non-thread-safe TypeUniverse javac, so a parallel minion deadlocks a spec
            // ("suite not green") and corrupts ~/.spock/RunHistory. Point Spock at a serialised project-local
            // config for the minions. Single-threaded pitest too (below), so the mutation gate is deterministic.
            jvmArgs = [
                    '-XX:+EnableDynamicAgentLoading',
                    "-Dspock.configuration=${project.file('spock-pitest.groovy').absolutePath}".toString(),
            ]
            // Task 4.3 (harden-engine-as-library): keep the mutable set to engine logic — drop generated Dagger
            // wiring and the pure debug/rendering classes that are also excluded from the jacoco 0.95 branch gate,
            // so both gates measure the same surface.
            excludedClasses = [
                    'io.github.joke.percolate.processor.Dagger*',
                    'io.github.joke.percolate.processor.internal.stages.dump.*',
                    'io.github.joke.percolate.processor.internal.graph.DotRenderer',
            ]
            // Mutation guards (change: harden-engine-as-library). Ratchet FLOORS — deliberately set below the
            // current measured score (killed ~90%, strength ~90% once the seam tests landed and Dagger/debug are
            // excluded) so the build fails on a regression but main is never red. failWhenNoMutations catches a
            // misconfigured run that mutates nothing.
            failWhenNoMutations = true
            mutationThreshold = 80
            coverageThreshold = 90
            testStrengthThreshold = 85
        }
        dependencies {
            pitest 'org.pitest:pitest-history-plugin:0.0.1'
            // @CoverageIgnore / @DoNotMutate escape hatches for pitest. compileOnly: they steer mutation analysis
            // only, never ship. Use sparingly — only where a mutant is genuinely un-killable, each one justified.
            compileOnly 'com.groupcdg:pitest-annotations:1.1.4'
        }
        // Task 4.3 (harden-engine-as-library): the mutation gate runs under check. Incremental analysis keeps a
        // warm repeat run fast (~50s cold on the unit suite); a score below the ratchet floor fails the build.
        tasks.named('check') { dependsOn 'pitest' }
    }

    pluginManager.withPlugin('jacoco') {
        tasks.withType(JacocoReport).configureEach {
            dependsOn test
            dependsOn tasks.matching { it.name == 'integrationTest' }
            executionData.from fileTree(buildDir).include('jacoco/*.exec')
            reports {
                xml.required = true
            }
        }
        tasks.withType(JacocoCoverageVerification).configureEach {
            violationRules {
                rule {
                    limit {
                        counter = 'BRANCH'
                        value = 'COVEREDRATIO'
                        // Per-module floor; defaults to 0.6. A module raises (or, while a ratchet is in
                        // flight, temporarily lowers) its own bar via ext.branchCoverageMinimum.
                        minimum = project.findProperty('branchCoverageMinimum') ?: 0.6
                    }
                }
            }
        }
        tasks.withType(Test).configureEach {
            finalizedBy jacocoTestReport
        }
        tasks.named('check') {
            dependsOn jacocoTestCoverageVerification
        }
    }

    pluginManager.withPlugin('pmd') {
        pmd {
            consoleOutput = false
            toolVersion = '7.24.0' // keep in sync with below
            rulesMinimumPriority = 5
            ruleSets = ["$rootDir/.pmd.xml"]
        }
    }

//    pluginManager.withPlugin('org.danilopianini.cpd') {
//        cpd {
//            toolVersion = '7.24.0' // keep in sync with above
//        }
//        tasks.named('cpdCheck') {
//            reports {
//                text.required = true
//                xml.required = true
//            }
//        }
//    }

    pluginManager.withPlugin('net.ltgt.errorprone') {
        pluginManager.apply 'net.ltgt.nullaway'
        dependencies {
            errorprone('com.google.errorprone:error_prone_core:2.49.0')
        }
        tasks.withType(JavaCompile).configureEach {
            options.errorprone {
                disableWarningsInGeneratedCode = true
            }
        }
    }

    pluginManager.withPlugin('net.ltgt.nullaway') {
        dependencies {
            errorprone('com.uber.nullaway:nullaway:0.13.4')
        }
        tasks.withType(JavaCompile).configureEach {
            options.errorprone {
                error('RequireExplicitNullMarking')
                nullaway {
                    error()
                    onlyNullMarked = true
                    treatGeneratedAsUnannotated = true
                    jspecifyMode = true
                    suggestSuppressions = true
                    checkContracts = true
                    acknowledgeRestrictiveAnnotations = true
                }
            }
        }
    }

    // Consumer-facing Maven publication. Applied only by modules that opt into 'maven-publish'.
    // The starter publishes as 'percolate'; every other publishable module as 'percolate-<name>'.
    pluginManager.withPlugin('maven-publish') {
        def publishedArtifactId = project.name == 'percolate' ? 'percolate' : "percolate-${project.name}"
        pluginManager.withPlugin('java') {
            publishing {
                publications {
                    maven(MavenPublication) {
                        artifactId = publishedArtifactId
                        from components.java
                        // Pin concrete versions resolved from the build, so the POM needs no version platform.
                        versionMapping {
                            usage('java-api') { fromResolutionResult() }
                            usage('java-runtime') { fromResolutionResult() }
                        }
                        // Drop the internal ':dependencies' platform's BOM-import: with versions pinned above the
                        // import is redundant, and keeping it would leak internal constraints onto consumers (and
                        // force us to publish the internal platform). Leaves consumer POMs self-contained.
                        pom.withXml {
                            asNode().dependencyManagement.each { it.parent().remove(it) }
                        }
                    }
                }
            }
        }
        pluginManager.withPlugin('java-platform') {
            publishing {
                publications {
                    maven(MavenPublication) {
                        artifactId = publishedArtifactId
                        from components.javaPlatform
                    }
                }
            }
        }
    }
}

wrapper {
    gradleVersion = '9.5.0'
}
