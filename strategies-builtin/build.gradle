plugins {
    id 'java'
    id 'groovy'
    id 'maven-publish'
    id 'io.freefair.lombok'
    id 'com.netflix.nebula.archrules.runner'
    id 'percolate.conventions'
    id 'info.solidsoft.pitest'
}

dependencies {
    annotationProcessor platform(project(':dependencies'))
    annotationProcessor 'org.projectlombok:lombok'
    annotationProcessor 'com.google.auto.service:auto-service'
    annotationProcessor 'io.github.joke.jspecify:processor'

    compileOnly platform(project(':dependencies'))
    compileOnly 'com.google.auto.service:auto-service-annotations'
    compileOnly 'org.projectlombok:lombok'
    compileOnly 'org.jspecify:jspecify'
    compileOnly 'org.jetbrains:annotations'

    implementation project(':spi')
    implementation project(':annotations')

    testImplementation platform(project(':dependencies'))
    testImplementation project(':annotations')
    testImplementation project(':processor')
    testImplementation project(':test-foundation')
    testImplementation 'com.google.testing.compile:compile-testing'
    testImplementation 'org.spockframework:spock-core'
    testImplementation 'org.jspecify:jspecify'
    testImplementation testFixtures(project(':spi'))
    testImplementation 'com.tngtech.archunit:archunit'

    testCompileOnly platform(project(':dependencies'))
    testCompileOnly 'org.jetbrains:annotations'

    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
    testRuntimeOnly 'org.mockito:mockito-junit-jupiter'
    testRuntimeOnly 'org.slf4j:slf4j-simple'

    testAnnotationProcessor platform(project(':dependencies'))
    testAnnotationProcessor 'io.github.joke.jspecify:processor'
    testAnnotationProcessor project(':percolate')
}

tasks.named('pmdTest') {
    exclude 'io/github/joke/percolate/docs/**'
}

tasks.named('compileTestJava') {
    options.release = 17
    options.compilerArgs << '-Apercolate.docTags=true'
}

tasks.named('integrationTest') {
    // ConstructionPreferenceDocExampleSpec and LiteralDirectiveReaderDocExampleSpec materialise the
    // builder-assembly and Extending pages' generated output here — see the equivalent declaration in
    // reactor/build.gradle for why an undeclared side-effect directory breaks a cache hit.
    outputs.dir layout.buildDirectory.dir('generated-doc-examples')
}
