plugins {
    id 'java-library'
    id 'maven-publish'
    id 'percolate.conventions'
}

dependencies {
    api project(':processor')
    api project(':strategies-builtin')
}

tasks.named('pitest') {
    enabled = false
}
