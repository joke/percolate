package io.github.joke.percolate.spi.types;

/** The kind of a {@link TypeDecl}: class, interface, or enum — the discriminations production code makes. */
public enum DeclKind {
    CLASS,
    INTERFACE,
    ENUM
}
