plugins {
    id 'java-library'
    id 'groovy'
    id 'java-test-fixtures'
    id 'maven-publish'
}

dependencies {
    annotationProcessor platform(project(':dependencies'))
    annotationProcessor 'org.projectlombok:lombok'

    compileOnly platform(project(':dependencies'))
    compileOnly 'org.projectlombok:lombok'
    compileOnly 'org.jspecify:jspecify'
    compileOnly 'com.google.auto.service:auto-service-annotations'

    implementation platform(project(':dependencies'))

    api platform(project(':dependencies'))
    api 'com.palantir.javapoet:javapoet'

    testFixturesImplementation platform(project(':dependencies'))
    testFixturesImplementation 'org.spockframework:spock-core'
    testFixturesImplementation 'org.apache.groovy:groovy'

    testImplementation platform(project(':dependencies'))
    testImplementation 'org.spockframework:spock-core'
    testImplementation 'org.apache.groovy:groovy'
    testImplementation 'org.jspecify:jspecify'
    testImplementation testFixtures(project(':spi'))

    testRuntimeOnly 'net.bytebuddy:byte-buddy'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

// TypeUniverse hosts a single shared JavacTask. Concurrent calls into javac
// internals (Types.isAssignable, Symbol.complete) trigger re-entrancy
// assertions like "Filling X during Y" on Java 21+ runtimes — SynchronizedElements
// only guards a subset of calls. Keep all test methods on one thread, same as
// strategies-builtin already does.
tasks.withType(Test).configureEach {
    maxParallelForks = 1
    systemProperty 'junit.jupiter.execution.parallel.enabled', 'false'
    systemProperty 'spock.parallel.threads', '0'
}
