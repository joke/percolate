plugins {
    id 'java'
    id 'groovy'
}

dependencies {
    annotationProcessor platform(project(':dependencies'))
    annotationProcessor 'org.projectlombok:lombok'
    annotationProcessor 'com.google.auto.service:auto-service'
    annotationProcessor 'com.google.dagger:dagger-compiler'

    compileOnly project(':annotations')

    compileOnly 'com.google.auto.service:auto-service-annotations'
    compileOnly 'org.jspecify:jspecify'
    compileOnly 'org.jetbrains:annotations'
    compileOnly 'org.projectlombok:lombok'

    implementation platform(project(':dependencies'))
    implementation project(':annotations')
    implementation 'org.jgrapht:jgrapht-core'
    implementation 'org.jgrapht:jgrapht-io'
    implementation 'com.google.auto:auto-common'
    implementation 'com.google.dagger:dagger'
    implementation 'com.palantir.javapoet:javapoet'

    testImplementation platform(project(':dependencies'))
    testImplementation 'com.google.testing.compile:compile-testing'
    testImplementation 'net.jqwik:jqwik'
    testImplementation 'org.spockframework:spock-core'
    testImplementation project(':processor-test-support')
    
    testRuntimeOnly 'net.bytebuddy:byte-buddy'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
    testRuntimeOnly 'org.mockito:mockito-junit-jupiter'

tasks.named('test') {
    jvmArgs += ['-Djqwik.database.directory=build/jqwik-database']
}

tasks.named('jacocoTestCoverageVerification') {
    enabled = false
}

//    dependencies {
//        codenarc platform(project.project(':dependencies'))
////        codenarc "org.apache.groovy:groovy:5.0.6"
//        codenarc 'org.codenarc:CodeNarc:3.7.0-groovy-4.0'
//    }
}
