package io.github.joke.percolate.processor.stages.generate;

import io.github.joke.percolate.spi.VarNames;

/**
 * Placeholder implementation for slice 1. The {@link VarNames} SPI interface is currently empty,
 * so this class has no methods beyond the interface contract.
 *
 * <p>Slice 3 will add a {@code fresh(String hint)} seam for lambda-parameter naming in strategies
 * that render lambdas (e.g., container scope transitions).
 */
final class VarNamesImpl implements VarNames {}
