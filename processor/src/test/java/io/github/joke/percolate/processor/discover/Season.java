package io.github.joke.percolate.processor.discover;

/** A fixture for {@code TypeSpaceAdapterSpec}: a non-JDK enum, exercising the discovery adapter's enum-kind branch. */
public enum Season {
    SPRING,
    SUMMER,
    FALL,
    WINTER
}
